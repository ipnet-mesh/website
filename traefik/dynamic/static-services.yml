http:
  routers:
    # Example static service configuration
    # Uncomment and modify as needed for non-Docker services

    # static-service:
    #   rule: "Host(`service.ipnt.uk`)"
    #   entryPoints:
    #     - websecure
    #   tls:
    #     certResolver: cloudflare
    #   service: static-service

    # api-service:
    #   rule: "Host(`api.ipnt.uk`)"
    #   entryPoints:
    #     - websecure
    #   tls:
    #     certResolver: cloudflare
    #   service: api-service

  services:
    # Example static services
    # Uncomment and modify as needed

    # static-service:
    #   loadBalancer:
    #     servers:
    #       - url: "http://192.168.1.100:3000"

    # api-service:
    #   loadBalancer:
    #     servers:
    #       - url: "http://192.168.1.101:8080"

# Example middleware configurations
# middlewares:
#   auth:
#     basicAuth:
#       users:
#         - "admin:$2y$12$..."  # Use htpasswd to generate
#
#   secure-headers:
#     headers:
#       accessControlAllowMethods:
#         - GET
#         - OPTIONS
#         - PUT
#       accessControlAllowOriginList:
#         - "https://ipnt.uk"
#       accessControlMaxAge: 100
#       addVaryHeader: true
